guard 'coffeescript', input: 'lib', bare: true, all_on_start: true
guard 'coffeescript', input: 'spec', bare: true, all_on_start: true

guard 'uglify', input: 'lib/fresh_url.js', output: 'dist/fresh_url.js' do
  watch 'lib/fresh_url.js'
end
